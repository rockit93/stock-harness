---
name: lark
description: 使用官方 lark-cli 操作飞书/Lark 的消息、群聊、文档、日历、任务、表格和知识库。
---

# 飞书系统能力

用户要求操作飞书时使用 `lark_cli`。先用 `<domain> --help` 或 `schema` 发现参数，优先使用 `+` 快捷命令。读取操作可直接执行；发送消息、创建/修改/删除内容等写操作必须符合用户明确意图，连接器未授权写入时不得绕过。高风险写操作必须先 dry-run，并仅在用户明确确认后传 `--yes`。不得输出凭据或令牌。
